node_modules
dist
.env
.env.*
!.env.example
.DS_Store
*.log
.vite
coverage
*.tsbuildinfo
.tsbuildinfo/
vite.config.js
vite.config.d.ts

# Real RaceBox sessions: multi-MB, kept locally only (see tests/fixtures/racebox/README.md)
tests/fixtures/racebox/*.csv

# Capacitor build artifacts (the native projects themselves stay in git)
ios/App/Pods/
ios/App/App.xcworkspace/xcuserdata/
android/.gradle/
android/.idea/
android/local.properties
android/app/build/
android/build/
android/captures/

# Impeccable live-mode runtime state (config.json is tracked, session journals are not)
.impeccable/live/sessions/
.impeccable/live/roots.json
.impeccable/live/inject-journal.json
